{
    bd = {
		dsn = ${SMIT_DSN}
	}

	kafka = {
	    host = ${KAFKA_HOST}
        port = ${KAFKA_PORT}
        batch_size = ${?KAFKA_BATCH_SIZE}
        topik = ${?KAFKA_TOPIK}
	}

	sentry_dsn: ${?SENTRY_DSN}

	tg = {
	    token = ${?TG_TOKEN}
        chat_id = ${?TG_CHAT_ID}
	}

	cors_origin_regex: "(http://|https://)?(.*\.)?(qa|stage|localhost|0.0.0.0)(\.ru)?(:\d+)?$"
}